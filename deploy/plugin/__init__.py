"""Hermes Web UI plugin entry point.

This plugin only contributes a dashboard tab (see dashboard/manifest.json);
it has no tools, hooks, or other CLI-side behavior to register.
"""


def register(ctx) -> None:
    pass
